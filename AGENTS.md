# skills

个人 skill 仓库。只收自己设计、经常用的流程。

每个 skill：`skills/<name>/SKILL.md`。`name` 即目录名，kebab-case（`a-z` `0-9` `-`，2–64）。

完成：frontmatter 含 `name` 与 `description`；本仓库可执行该 skill。

本文件只放仓库约定。skill 正文只在该 skill 自己的文件里。同流程只保留一个目录。
